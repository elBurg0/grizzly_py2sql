# load connection
con = ...

# load table
table = Table(con, "table")
table = grizzly.read_table("events")


def udf():
    if table.column > 10
    table.column = 12

UPDATE table
SET table.column = 12
WHERE table.column > 12


# Bei return auch SQL-Return